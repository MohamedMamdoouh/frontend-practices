# Angular Demo3

Angular CLI application in `angular/Demo3`.

## Getting started

1. Run `npm install`
2. Run `ng serve`
3. Open `http://localhost:4200/`

## Notes

This demo contains the Angular app source in `src/` and configuration files.
