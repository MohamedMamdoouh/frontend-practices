# Angular Demo2

Angular CLI application in `angular/Demo2`.

## Getting started

1. Run `npm install`
2. Run `ng serve`
3. Open `http://localhost:4200/`

## Notes

This demo contains the Angular app source in `src/` and standard Angular CLI files.
