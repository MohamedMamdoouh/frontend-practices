import { GetNumberToPowerPipe } from './get-number-to-power-pipe';

describe('GetNumberToPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNumberToPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
