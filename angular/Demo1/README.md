# Angular Demo1

Angular CLI application in `angular/Demo1`.

## Getting started

1. Run `npm install`
2. Run `ng serve`
3. Open `http://localhost:4200/`

## Notes

This demo contains the Angular source files in `src/` and standard Angular CLI configuration files.
