# TypeScript Examples

TypeScript folder with compiler configuration and example source files.

## Contents

- `tsconfig.json` - TypeScript compiler settings
- `src/` - TypeScript examples and sample files

## Notes

Use `npm install` and `tsc` to compile files in `typescript/src`. This folder focuses on type-safe code, interfaces, classes, and modern TypeScript patterns.
